# redis-sentinel-config-demo
A configuration demo for redis sentinel.

## Attention
Before running with these configuration files, you need to check all of them and make directories for these configuration files.
